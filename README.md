








QUESTION 3 
In question 3 turtle graphics library of python is used for creating the geometric patterns.A recursive functiion draw_edge is used for recursively drawing the patterns in inside draw_edge 
we have create a condition that if the value of depth is 0 then the linne will be a straight line else the line will be divided into 3 equal segments of 1/3rd length 
To run the program we have to make use of the terminal and run the script as 
python 3.py
